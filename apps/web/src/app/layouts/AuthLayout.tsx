import type { PropsWithChildren } from 'react'

export function AuthLayout({ children }: PropsWithChildren) {
  // TODO: centered RTL auth layout for login and session recovery screens.
  return children
}
